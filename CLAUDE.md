# 이번주맛 (This Week Taste) — 주간 식음료 신상품 모아보기

> ⚠️ 서비스명은 아직 확정이 아니다. '이번주맛'은 플레이스홀더다.
> 코드/폴더/변수명에 서비스명을 하드코딩하지 말 것.
> 표시용 이름은 `web/config/site.ts` 한 곳에서만 관리한다.

> 이 파일은 Claude Code가 매 세션 자동으로 읽습니다.
> 여기 적힌 규칙은 개별 요청보다 우선합니다. 규칙을 어겨야 할 이유가 생기면
> 코드를 고치기 전에 먼저 이 파일 수정을 제안하세요.

> **이 파일은 명령만 적는다.** 모든 문장은 "~한다 / ~하지 않는다"로 끝난다.
> 왜 그렇게 정했는지와 무엇을 버렸는지는 [`docs/adr/`](docs/adr/)에,
> 측정한 사실은 [`docs/RECON_REPORT.md`](docs/RECON_REPORT.md)에 있다.
> 여기에 "~이기 때문이다"가 두 줄 넘게 이어지면 ADR로 옮길 신호다.

## 1. 프로젝트가 하는 일

매주 한국의 마트 / 편의점 / 카페 / 디저트 / 프랜차이즈 음식점 / 식품 대기업 자사몰에서
새로 나온 식음료 제품을 자동으로 수집해, 주 단위로 정리된 웹사이트로 발행한다.

**범위는 포장된 식품·음료 공산품과 프랜차이즈 카페·매장의 신메뉴다.** 생활용품·잡화,
신선 원물(과일·채소·정육·수산물·쌀), 의약외품, 건강기능식품 정제/캡슐, 완구·캐릭터
상품은 넣지 않는다. **소스가 주는 전부를 긁지 않는다** — 2.1의 "전체 카탈로그"는
*우리가 추적하는* 카탈로그이지 소스가 파는 전부가 아니다([ADR-0012](docs/adr/0012-collection-scope.md)).

**핵심 목표: 매주 운영에 드는 시간을 최소화한다.** 판단 기준은 하나다 —
**자동으로 할 수 있는 일에는 사람을 넣지 않는다. 사람은 판단이 필요한 곳에만 넣는다.**

숫자 상한은 두지 않는다. 상한이 있으면 목표가 아니라 금지선으로 작동해서, 사람이
잠깐이라도 봐야 하는 기능이 설계 단계에서 잘려나간다([ADR-0009](docs/adr/0009-weekly-effort-goal.md)).

이 기준은 **수집 파이프라인에** 적용된다. 서빙 계층(댓글, 후기, API)은 사람이 쓰는
곳이라 성격이 다르다. 둘을 같은 잣대로 재면 서빙 쪽이 영원히 만들어지지 않는다.

### 이 프로젝트가 겸하는 것

포트폴리오다. 그래서 "가장 적은 운영 부담"이 아니라 **"설명할 수 있는 설계"**가
우선한다. 둘이 충돌하면 후자를 택하고, 왜 택했는지를 문서와 커밋에 남긴다.
`learning/`이 그 기록이다.

## 2. 절대 원칙 (Non-negotiable)

### 2.1 신상품은 "차집합"으로 판별한다
소스 사이트의 '신상품' 라벨이나 '최신순' 정렬을 신뢰하지 않는다.
매주 **전체 카탈로그 스냅샷**을 뜨고, 지난주 스냅샷과 비교해
- 이번 주에만 있는 항목 → 신상품
- 지난주에만 있는 항목 → 단종 후보
- 양쪽에 있으나 이름·가격이 달라진 항목 → **변경**(신상 아님)
- 같은 제품인지 확신할 수 없는 항목 → **보류**. added/removed 어느 쪽에도 넣지 않고 따로 뺀다

뒤의 두 종류가 없으면 이름이 한 글자 바뀐 제품이 "단종 1건 + 신상 1건"으로 발행된다.
같은 제품인지 판정하는 근거는 소스마다 다르므로 `diff.py`에 계층으로 적는다(4장 `alt_ids`).

이유: 소스마다 신상 표기 규칙이 다르고 수시로 바뀐다. 이름/가격만 안정적으로
긁으면 되는 diff 방식이 훨씬 덜 깨진다.

### 2.2 수집 데이터는 파일에, DB는 서빙 계층에만
경계가 있다. 이 선을 넘나들면 설계가 무너진다.

| | 저장소 | 어디에 | 이유 |
|---|---|---|---|
| 원본 응답 `raw/` | **파일** | 비공개 | 재처리용 아카이브. 조회하지 않는다 |
| 스냅샷 `snapshots/` | **파일** | 비공개 | 조회 패턴이 "주차별 전체 읽기" 하나뿐이다 |
| 중간 산출물 `diffs/`, `enriched/` | **파일** | 비공개 | 재생성된다 |
| 발행물 `data/weeks/` | **파일** | **공개** | 요약 + 원문 링크라 7장이 허용한 형태다 |
| 후기, 댓글 | **DB** | — | 낱개 조회, 검색, 쓰기가 생긴다 |

**"어디에" 열은 저장소 공개 범위다.** 코드 저장소는 공개이므로 소스의 전체 카탈로그와
응답 원본은 여기 둘 수 없다 — 7장이 금지한 재배포가 된다. 비공개 데이터 저장소에 두고
`THIS_WEEK_TASTE_DATA_DIR`로 가리킨다. 경로는 `pipeline/paths.py` 한 곳이 정한다.
발행물만은 코드 저장소에 남는다(웹 빌드가 상대 경로로 읽는다). 근거는 [ADR-0010](docs/adr/0010-repo-public-scope.md).

**금지: 스냅샷과 원본을 DB로 옮기는 것.** 파이프라인은 DB를 몰라야 한다.
`snapshot.py`, `diff.py`, `enrich.py`는 파일만 읽고 쓴다. DB는 발행 이후의 세계다.

판단 기준과 버린 대안은 [ADR-0003](docs/adr/0003-storage-boundary.md).

### 2.3 소스는 서로 격리한다
크롤러 하나가 실패해도 나머지는 정상 발행되어야 한다.
- 소스별 파일 1개: `scrapers/<source_id>.py`
- 소스별 출력 1개: `data/snapshots/<YYYY-Www>/<source_id>.json`
- 한 소스의 예외가 파이프라인 전체를 중단시키지 않는다

### 2.4 실패는 시끄럽게 (Fail loudly)
가장 나쁜 결과는 "조용히 빈 페이지가 발행되는 것"이다.
- 크롤러 결과가 0건이거나 직전 주 대비 70% 이상 **감소** → 이상 상황으로 간주
- 직전 주 대비 200% 이상 **증가**도 이상 상황이다. 감소만 보면 목록의 성격이 통째로
  바뀐 사고(행사 교체, 카테고리 개편)를 놓친다
- 이상 상황 발생 시: 해당 소스는 **지난주 데이터를 그대로 유지**하고,
  GitHub Issue를 자동 생성한다
- **부분 수집은 실패로 간주한다.** 한 소스가 카테고리 N개로 나뉘어 있을 때 1개라도 실패하면
  그 소스의 스냅샷 전체를 폐기하고 지난주 것을 유지한다. 부분 스냅샷으로 diff를 돌리면
  빠진 카테고리가 통째로 "단종"으로 잡힌다. 2.3의 격리 단위는 소스이지 카테고리가 아니다
- `except: pass` 절대 금지. 예외는 삼키지 말고 로그에 남기고 위로 던진다

### 2.5 원본을 항상 보관한다
파싱 전 원본 응답(HTML/JSON)을 `<데이터 디렉토리>/raw/<YYYY-Www>/<source_id>/<요청식별자>.<ext>`에
저장한다(2.2의 "어디에" 참조. 기본값은 저장소의 `data/`다).
파서가 깨졌을 때 원본이 있으면 재처리가 가능하고, 없으면 그 주는 영영 잃는다.
(용량이 문제되면 90일 이후 압축/삭제하는 정리 잡을 별도로 둔다.)

> 소스당 응답이 1개인 경우는 거의 없다. CU만 해도 카테고리 6 × 페이지 = 110개다.
> `<요청식별자>`는 재현에 필요한 파라미터를 담는다(CU: `<카테고리>_<페이지>`).

### 2.6 한 주차의 스냅샷은 한 번만 뜬다
스냅샷이 이미 있으면 `snapshot.take()`는 다시 긁지 않고 그것을 쓴다. 다시 뜨려면
`--refresh`(`make ... REFRESH=1`)를 명시한다.

수집은 봇이 월요일에, 발행은 사람이 나중에 로컬에서 돌린다(8장). 그래서 **재수집은
이미 발행한 주차의 근거를 조용히 교체한다.** 실제로 2026-W35가 그랬다 — 14:24에
발행하고 15:30에 봇이 다시 수집했다.

- 재사용은 로그로 **시끄럽게** 알린다. 조용히 건너뛰면 다시 긁은 줄 알게 된다(2.4)
- **이월본(`held_from`)은 재사용하지 않는다.** 이월은 "이번 주 데이터가 없다"는 뜻이므로
  그걸 굳히면 지난주 카탈로그가 이번 주의 최종 스냅샷이 된다
- 발행 리포트의 `by_source.<소스>.snapshot`에 **무엇을 보고 발행했는지**를 남긴다.
  멱등이 어긋남을 막고, 이 값이 못 막은 어긋남을 보이게 한다

근거와 버린 대안은 [ADR-0011](docs/adr/0011-snapshot-once-per-week.md).

## 3. 디렉토리 구조

```
.
├─ CLAUDE.md
├─ sources/targets.yml        # 타깃 브랜드 정의 (단일 진실 공급원)
├─ scrapers/
│   ├─ base.py                # 공통 HTTP 세션, 재시도, 레이트리밋
│   ├─ cu.py
│   ├─ gs25.py
│   └─ ...
├─ pipeline/
│   ├─ weeks.py               # ISO 주차 계산 (KST). 날짜 포맷의 단일 지점
│   ├─ paths.py               # 수집 데이터가 사는 곳. 경로의 단일 지점
│   ├─ snapshot.py            # 전체 카탈로그 수집 → data/snapshots/
│   ├─ diff.py                # 지난주 대비 차집합 계산
│   ├─ enrich.py              # diff 결과(신상)만 상세 페이지 보강
│   ├─ curate.py              # Claude API로 정제/분류/한 줄 설명
│   └─ publish.py             # data/weeks/<YYYY-Www>.json 생성
├─ data/                      # 여기까지가 파일의 세계 (2.2)
│   ├─ published/             # 소스별 부분 산출물. merge의 입력. 커밋하지 않는다
│   └─ weeks/                 # 발행용 최종 산출물 (사이트가 읽는 파일). **공개**
│
│  ── 아래는 비공개 데이터 저장소에 있다 (THIS_WEEK_TASTE_DATA_DIR) ──
│     raw/        원본 응답
│     snapshots/  정규화된 전체 카탈로그
│     diffs/      지난주 대비 차집합
│     enriched/   상세 보강 결과
├─ tests/
│   └─ fixtures/              # 골든 테스트용 저장 응답. 네트워크 없이 돈다
├─ docs/                      # 정찰 결과, 배포 방법, 되돌릴 것 목록
├─ learning/                  # 학습 기록. raw/ 는 커밋하지 않는다
├─ web/                       # Next.js
│   └─ app/about/             # 크롤러 UA가 가리키는 페이지. 없으면 UA가 거짓말이 된다
├─ .github/workflows/weekly.yml   # 매주 수집만 한다. 발행은 사람이
└─ .claude/                       # 작업 도구 설정. **공개 저장소에 둔다**
```

**`.claude/`도 규칙 표면이다.** 공개 저장소에 있으므로 CLAUDE.md나 ADR이 바뀌면
`skills/`를 **같은 커밋에서** 고친다. 어긋난 채로 두면 낡는 것이 아니라 거짓말이 된다
(ADR-0013이 그 사례다 — 되돌린 규칙이 거기 살아 있었다).
훅에는 **테스트 실행 이상의 것을 넣지 않는다.** 저장소를 클론한 사람의 기계에서
자동으로 실행된다. 개인 설정(`settings.local.json`)은 커밋하지 않는다.

**`enrich.py`가 왜 따로 있나**: 목록 페이지는 이름·가격·이미지까지만 준다. 설명문과 태그는
상세 페이지에만 있는 소스가 많다(CU 확인). 전체 카탈로그의 상세를 매주 긁는 것은 요청 수가
수천 배로 뛰어 불가능하지만, **diff가 걸러낸 신상 수십~수백 건만은 감당 가능**하다.
이 단계가 있어야 `blurb`를 LLM이 창작하지 않고 요약만 하게 된다(6장과 직결).
따라서 순서는 `snapshot → diff → enrich → curate → publish`다.

단 **모든 소스가 그렇지는 않다.** 스타벅스는 목록 응답이 설명문을 전부 준다(326/326).
그런 소스는 스냅샷의 `description`을 채우고 `enrich`는 건너뛴다(4장). 이미 가진 것을
버리고 다시 긁는 것은 소스 서버에 대한 예의도 아니다.

## 4. 데이터 스키마

### 스냅샷 항목 (scrapers 출력)
```jsonc
{
  "source_id": "cu",
  "external_id": "8801234567890",   // 소스의 안정적 키. 없으면 name의 해시
  "alt_ids": { "gd_idx": "17620" },  // 소스가 키를 둘 이상 줄 때. diff의 보조 매칭 키
  "name": "백종원 매콤제육덮밥",
  "price": 4900,                     // 원, 미상이면 null
  "category_raw": "간편식사",        // 소스에서 유래한 카테고리 문자열. 미상이면 null
  "description": null,               // 목록 응답이 설명문을 주면 여기. 아니면 null
  "tags": [],                        // 목록 응답이 태그를 주면 여기. 아니면 []
  "image_url": "https://...",
  "source_url": "https://...",
  "scraped_at": "2026-08-06T09:00:00+09:00"
}
```

`description`은 **목록 응답에 설명문이 있는 소스만** 채운다. 소스가 안 주면 `null`이고,
그때 `enrich.py`가 상세 페이지를 긁어 채운다. 지어내지 않는다(6장).

`tags`도 같은 계약이다. **소스가 주면 채우고, 안 주면 `[]`다.** 배스킨라빈스가 목록에
`#크림치즈 #조청카라멜` 형태로 준다(2026-08-25 확인). 이 필드가 없으면 그 태그를 버리고
LLM이 이름만 보고 지어내게 되는데, 그건 6장이 금지하는 창작이다.

⚠️ **`description`과 `tags`가 같은 곳에서 온다고 가정하지 않는다.** 배스킨라빈스는
태그를 *목록*이, 설명문을 *상세*가 준다. 그래서 `enrich.py`는 상세를 긁을 때
**스냅샷이 이미 가진 태그를 보존**한다 — 상세가 태그를 안 주는 것은 실패가 아니다.

이 필드가 있는 이유: 3장은 "설명문은 상세에만 있다"를 전제로 `enrich.py`를 두었는데,
그 전제가 안 맞는 소스가 있다. 스타벅스는 목록 응답이 326건 전부에 설명문을 준다.
필드가 없으면 이미 손에 든 설명문을 버리고 상세를 326번 다시 긁어야 한다.
**설명문이 어디서 왔든 파이프라인 뒷단은 같은 자리에서 읽는다** — 이것이 이 필드의 계약이다.

`category_raw`는 **응답 본문에 카테고리가 있으면 그 원문**을, 없으면 **우리가 요청에 쓴
카테고리 값에서 역산한 이름**을 넣는다(CU가 이 경우다). 계층이 여러 단계면 `>`로 잇고,
한 단계뿐이면 구분자를 쓰지 않는다. 어느 쪽인지는 소스별 스크래퍼 주석에 남긴다.

`alt_ids`는 소스가 안정적 키를 둘 이상 줄 때만 채운다. `external_id` 하나가 바뀌어도
다른 키로 같은 제품임을 알아내기 위한 것이며, `diff.py`가 이 값을 매칭 계층에 쓴다.

> ⚠️ **`external_id`는 소스의 전체 카탈로그에서 유일해야 한다. 추측하지 말고 실측할 것.**
> 주키는 *물리적 제품*이 아니라 *카탈로그 항목*을 가리키는 키로 잡는다. 우리가 발행하는
> 단위가 카탈로그 항목이기 때문이다. 소스 간 대조에 쓸 수 있는 키(바코드 등)는 `alt_ids`에 넣는다.
> `snapshot.py`가 매 실행마다 유일성을 검사하고, 깨지면 발행을 멈춘다.
> 실측 근거는 [ADR-0001](docs/adr/0001-product-id.md).

### 발행 항목 (curate 이후)
```jsonc
{
  "id": "cu--8801234567890",         // first_seen 주차에 확정. 이후 절대 바꾸지 않는다
  "week": "2026-W32",
  "brand": "CU",
  "channel": "convenience",          // mart|convenience|cafe|dessert|restaurant|fmcg
  "name": "백종원 매콤제육덮밥",
  "price": 4900,
  "category": "도시락",              // 정규화된 자체 분류
  "tags": ["매운맛", "간편식"],      // 소스에 없고 enrich도 실패하면 []
  "blurb": "한 줄 설명",             // 소스 설명문의 요약. 근거가 없으면 null
  "image_url": "...",
  "source_url": "...",
  "source_id": "cu",                 // 어느 소스에서 왔는가
  "external_id": "8801234567890",    // 스냅샷 원본 키를 그대로 보존
  "alt_ids": { "gd_idx": "17620" },
  "first_seen": "2026-W32",
  "last_seen": "2026-W32",
  "status": "active"                 // active|discontinued
}
```

`id`는 **first_seen 주차에 확정하고 그 뒤로 절대 바꾸지 않는다.** 나중에 더 좋은 키를 알게 돼도
유지한다. id가 바뀌면 아카이브 URL이 깨지고, diff가 같은 제품을 "단종 1 + 신상 1"로 오탐한다.
`external_id`/`alt_ids`를 별도로 보존하는 이유도 같다 — **매칭은 원본 키로 하고 `id`를 역파싱하지 않는다.**

`source_id`가 있는 이유: 한 주차 파일에 소스가 여럿 들어간다. `external_id`는 **소스 안에서만**
유일해서 소스끼리는 겹칠 수 있고(CU의 `gd_idx`와 오리온의 `goodsno`가 둘 다 숫자다),
소스로 좁히지 않고 `external_id`로 지난주 항목을 찾으면 **다른 소스의 `id`와 `first_seen`을
물려받는다.** 예외 없이 조용히 틀린다.

`blurb`와 `tags`는 nullable이다. 소스가 설명문을 주지 않고 enrich도 실패했다면 `blurb`는 `null`,
`tags`는 `[]`로 두고 그대로 발행한다. **채우려고 지어내지 않는다**(6장).

`week`는 ISO 주차 형식(`YYYY-Www`)을 쓴다. 사이트 URL도 `/week/2026-W32`로 맞춘다.
이 값이 프로젝트 전체의 척추다. 다른 날짜 포맷을 섞지 않는다.

## 5. 크롤러 작성 규칙

- **먼저 내부 API를 찾는다.** 브라우저 개발자도구 Network 탭에서 JSON을 반환하는
  XHR을 찾으면 그걸 직접 호출한다. Playwright는 최후의 수단이다 (느리고 잘 깨짐).
- 요청 간격 최소 1초. 동시 요청 금지.
- `robots.txt`를 확인하고 존중한다. 막힌 경로는 크롤링하지 않는다.
  **`Crawl-delay`와 `Visit-time`도 존중한다.** 둘 다 `base.Session`이 강제하므로
  스크래퍼가 따로 할 일은 없다. 창 밖에서 돌리면 시끄럽게 거부된다([ADR-0014](docs/adr/0014-collection-time-window.md)).
- User-Agent에 연락 가능한 식별자를 넣는다: `ThisWeekTaste/1.0 (+https://<도메인>/about)`
- 타임아웃 15초, 실패 시 지수 백오프로 최대 3회 재시도.
- 파서는 방어적으로 쓴다. 필수 필드는 `name` 하나뿐이고, 나머지는 없으면 `null`.
- 각 스크래퍼는 `fetch() -> list[dict]` 형태의 함수 하나를 노출한다.
  단독 실행(`python -m scrapers.cu`)하면 결과를 stdout에 찍도록 한다.

### 소스 하나를 붙일 때 손대는 곳 (4단계용 체크리스트)

```
sources/targets.yml            status를 verified로, method·url 확정
scrapers/<id>.py               fetch() + parse_*() (파싱은 네트워크와 분리해 테스트 가능하게)
                               CATEGORIES + BOOTSTRAP_COUNTS(첫 수집 검증용 실측치)
tests/test_<id>_parse.py       tests/fixtures/의 저장된 응답으로 골든 테스트
pipeline/sources.py            표에 한 줄: brand·channel·detail·monotonic_key
```

`pipeline/`에서 고칠 곳은 **표 한 줄뿐이다.** 소스별 분기는 2026-08-13에
`pipeline/sources.py` 하나로 모았다. 스크래퍼 모듈은 `scrapers/<source_id>.py`
규칙으로 찾으므로 등록할 필요가 없다. 표에 없는 값(카테고리 코드, 부트스트랩 건수)은
소스의 *내용*이라 스크래퍼 파일이 갖는다 — `pipeline/`에 두면 7장이 말하는 누수다.

`external_id`로 쓸 키는 **전체 카탈로그를 한 번 뜬 뒤 유일성을 실측하고 정한다.**
그럴듯한 키가 실제로는 중복되는 일이 있다(4장 경고 참조).

### 그리고 공유 코드가 이 소스의 모양을 견디는지 확인한다

소스 1개로 짠 코드에는 그 소스의 가정이 박혀 있고, 새 소스를 붙일 때마다 하나씩 드러난다.
**테스트는 이미 겪은 것만 지킨다.** 아직 안 겪은 것은 여기서 사람이 묻는다.

- 이 소스가 주는 키 이름을 `diff.py`가 아는가 (`alt_ids` 이름을 하드코딩하지 않는가)
- 가격이 없는 소스인가 — 그러면 diff의 (이름, 가격) 계층이 무력해진다
- 같은 이름이 두 번 나오는가 — 그러면 이름 해시로는 가를 수 없다
- 카테고리가 없는 소스인가 — `snapshot.py`의 건수 검증이 카테고리를 전제한다

답이 "그렇다"인 항목이 나오면 **그때 테스트를 쓴다.** 그래야 다음부터 기계가 지킨다.

## 6. LLM 편집 단계 (`pipeline/curate.py`) 규칙

- 사용 모델: **Haiku 4.5로 시작**. 품질이 부족하면 Sonnet 5로 승격.
  Opus / Fable은 이 단계에 쓰지 않는다 (비용 대비 이득 없음).
- 역할은 **판단**이지 **창작**이 아니다. LLM에게 시키는 일:
  1. 진짜 신제품 vs 리뉴얼/재입고/용량변경 구분
  2. 동일 제품의 중복 항목 병합 (예: 같은 상품의 낱개/멀티팩)
  3. 자체 카테고리 및 태그 부여 (분류 목록은 **채널마다 다르다** — [ADR-0006](docs/adr/0006-category-taxonomy-per-channel.md))
  4. **소스의 공식 설명문을 40자 이내로 요약** (창작이 아니라 압축이다)
  5. **범위 밖 판정** (`out_of_scope`). 카테고리로 못 가르는 잔여를 거른다 — CU는 상위
     6개 카테고리뿐이라 `식품` 안의 사과, `과자류` 안의 키링을 코드로 못 뺀다.
     **기본값은 `false`(포함)다.** 판정이 없거나 LLM이 실패하면 빠지지 않고 실린다 —
     한 건 더 실리는 쪽이 사라지는 쪽보다 눈에 띈다([ADR-0012](docs/adr/0012-collection-scope.md))
  6. 후기 글이 **홍보인지 일반 후기인지 판별** (v2. "협찬", "제공받아" 같은 표기와 문체를 본다)
  7. 보도자료 본문에서 **제품 레코드 추출** (press 소스용. 아직 붙이지 않았다)

  5~7번은 앞의 넷과 성격이 다르다. 1~4는 이미 정형화된 레코드를 다듬는 일이라 틀려도
  표시가 어긋나는 정도지만, 뒤의 셋은 **레코드의 존재 자체를 바꾼다** — 5번은 없애고
  6·7번은 만들어낸다. 그래서 셋 다 결과를 그대로 발행하지 않고 근거를 남긴다.
  5번은 제외한 항목의 이름을 `by_source.<소스>.out_of_scope.names`에 남긴다.

  ⚠️ **프롬프트에서 이 지시의 자리가 품질을 바꾼다.** `out_of_scope`를 1번 자리에
  길게 넣었더니 모델이 `name`을 빼먹기 시작해 blurb가 93 → 72건으로 떨어졌다.
  뒤로 내리고 "다섯 필드를 전부 넣는다"를 명시하니 93건으로 돌아왔다(2026-08-24 실측).
- 출력은 **JSON만**. 프롬프트에 "설명이나 마크다운 없이 JSON만 반환" 명시.
- 파싱 실패 시 1회 재시도, 그래도 실패하면 **LLM 없이 원본 그대로 발행**한다.
  편집 품질보다 발행 자체가 우선이다.
- LLM에 없는 정보(맛, 후기, 출시 배경)를 지어내게 하지 않는다. 프롬프트에 명시.
- **enrich 단계가 설명문을 못 가져왔으면 `blurb`는 `null`이다.** 이름만 보고 쓰게 하지 않는다.
  특히 CU는 제품명이 소스에서 12자로 잘려 오므로, 이름을 근거로 한 설명은 전부 추측이 된다.
- **`name`은 LLM이 건드리는 필드가 아니다.** 잘린 이름을 "복원"시키지 않는다.
  출력의 `name`이 입력과 다르면 그 항목은 LLM 결과를 버리고 원본을 쓴다.
- **상품 키(`external_id`)를 LLM에 보내지 않는다.** 배치 안에서만 유효한 참조
  (`r0`, `r1`, …)로 주고받고 우리가 되돌린다. 홈플러스의 `070234705`처럼 앞자리가
  0인 키는 모델이 숫자로 해석하거나 0을 잃어, **그 소스가 통째로 편집되지 않는다.**
  발행은 원본으로 폴백되어 계속되므로 조용하다. 자세한 것은 [ADR-0006](docs/adr/0006-category-taxonomy-per-channel.md).

## 7. 하면 안 되는 것

- ❌ 제품 이미지를 우리 서버에 복제 저장 — 원본 URL 참조 또는 보도자료 이미지만 사용
- ❌ 로그인이 필요한 페이지 크롤링
- ❌ 소스 사이트의 데이터를 통째로 재배포하는 형태의 페이지 (반드시 요약 + 원문 링크)
- ❌ 사용자 계정 / 로그인 기능 (v1 범위 밖. v2에서 댓글과 함께 들어온다)
- ❌ 추상화 레이어 선행 구축. 소스 3개까지는 복붙을 허용하고, 4개째에 공통화한다.
  **단 이것은 코드 중복 얘기이지 어휘 얘기가 아니다.** 아래와 헷갈리지 말 것.
- ❌ **소스 고유 어휘가 `pipeline/`에 새는 것.** `scrapers/cu.py`에 `gd_idx`가 있는 건
  당연하지만 `pipeline/publish.py`에 있으면 누수다. 모든 소스가 쓰는 파일이기 때문이다.

  판별법: **`pipeline/` 안에서 특정 소스의 단어가 보이면 의심한다.** 허용되는 자리는
  **소스별 값을 담는 표 하나뿐**이다(`BRANDS`, `_monotonic_key`, `_scraper_for`의 분기).
  로직이나 필드 이름에 박으면 안 된다.

  실제로 그래서 터졌다 — `publish.py`의 리포트가 `gd_idx_monotonic`이라는 필드로
  단조성을 보고했는데, 그 키가 없는 소스에서는 신상 전량이 "등록 순서를 거스름"으로
  집계됐다. 없는 오탐을 만들어낸 것이다. **필드 이름 자체가 이미 경고였다.**

  그리고 누수는 방치되면 낡는 게 아니라 **거짓말이 된다.** `publish.py` docstring은
  id를 "바코드가 있으면 바코드"라고 설명하고 있었는데, [ADR-0001](docs/adr/0001-product-id.md)이
  그 결정을 뒤집은 뒤에도 그대로였다.
- ❌ 테스트를 위한 테스트. 테스트는 **틀려도 조용한 곳**에만 작성한다. 두 종류다:
  1. **외부에서 들어온 값의 모양에 의존하는 코드** — 소스 HTML 파서, LLM 응답 파싱.
     남이 언제든 바꿀 수 있고, 어긋나면 예외 대신 빈 결과가 나온다
  2. **틀린 답이 예외 없이 그냥 숫자로 나오는 코드** — `diff.py`, 정규화 로직

  기준을 파일 목록이 아니라 성질로 적는 이유는, 목록은 파일이 늘 때마다 고쳐야 하고
  고치는 걸 잊으면 규칙이 현실과 어긋나기 때문이다.

  파서는 **저장된 원본 샘플을 쓰는 골든 테스트**로 검증한다. 네트워크 없이 돌고,
  소스 사이트가 바뀌었을 때 무엇이 어떻게 달라졌는지 보여주는 유일한 장치다.
  픽스처는 `tests/fixtures/`에 커밋한다(`scratch/`는 gitignore 대상이라 근거를 둘 수 없다).

### 실행 전에 승인을 받는 것

아래는 금지가 아니라 **혼자 결정하지 않는 것**이다. 되돌리기 어렵거나, 한번 나가면
회수할 수 없거나, 사람의 신상에 닿는다.

- **User-Agent 연락처** — 5장이 연락 가능한 식별자를 요구하는데, 이 값은 매주 수백 번
  소스 회사 서버 로그에 남는다. 개인 이메일을 넣지 않는다. 도메인 등록 시 whois 보호를 켠다
- **저장소 공개 범위** — 무엇을 공개 저장소에 올리는가 (2.2)
- **새 소스의 이용약관** — robots.txt는 `base.Session`이 강제하지만 약관은 사람이 읽어야 한다
- **이미지 사용 방식** — 지금은 원본 CDN 참조다. 남의 대역폭을 쓴다
- **`/about` 페이지 내용** — UA가 가리키는 곳이다

요청량 자체는 검토가 끝났다. 간격 1초 강제 + 동시 요청 없음이라 사람 한 명이
쇼핑몰을 둘러보는 것보다 적다. 실제 위험은 요청량이 아니라 약관과 봇 차단이다.

## 8. 개발 순서 (이 순서를 지킨다)

1. **정찰** — 타깃 사이트 구조 조사, 응답 샘플을 `scratch/`에 저장 (코드 작성 없음)
2. **수직 관통** — 소스 1개(CU)로 스냅샷 → diff → 발행 → 정적 페이지까지 전 구간 연결
3. **2주 검증** — 실제로 2주 연속 돌려 diff가 의미 있는 결과를 내는지 확인
4. **확장** — 소스를 우선순위(P0 → P1 → P2) 순으로 추가
5. **자동화** — GitHub Actions cron + 실패 알림
6. **UI 개선** — 필터, 검색, 아카이브
7. **서빙 계층** — DB, API, 후기 링크, 댓글 (2.2의 경계를 지킨다)

**3단계는 *발행*의 게이트이지 *수집*의 게이트가 아니다.** diff 품질이 확인되기 전에는
발행하지 않는다. 하지만 스냅샷은 미리 뜬다. 어떤 소스든 첫 스냅샷은 비교 대상이 없어
구조적으로 발행이 불가능하므로, 추가를 미루면 그 소스의 첫 결과가 그만큼 밀린다.

근거와 버린 대안은 [ADR-0004](docs/adr/0004-source-expansion-gate.md).

### 현재 위치: 4단계 확장 (2026-08-26 기준)

**소스 17개**로 전 구간이 연결되어 실제 데이터로 돌아간다. W35 스냅샷 9,839건.
2026-08-25에 dessert·restaurant 채널이 열려 **6채널 전부**에 소스가 있다.
restaurant는 7곳이 되어 채널 중 소스가 가장 많다.

```
make setup     # .venv + npm install (최초 1회)
make test      # 네트워크 없이 돈다
make week      # snapshot → diff → enrich → publish (소스 1개, 부분 산출물까지)
               # 이미 있는 스냅샷은 다시 긁지 않는다. 다시 뜨려면 REFRESH=1 (2.6)
make week-all  # 등록된 소스 전부 + 병합. 하나가 실패해도 나머지는 계속 간다 (2.3)
make merge     # 소스별 부분 산출물 → 사이트가 읽는 파일
make site      # web/out/ 정적 빌드
make check-images  # 발행물 이미지가 실제로 열리는지 표본 검사 (배포 전)
```

**자동화 경계**: Actions는 `make collect-all`(스냅샷 → diff)까지만 돌리고
**발행은 사람이 한다.** `curate.py`를 구독 인증(`claude -p`)으로 돌리고 있어 CI에서는
안 되기 때문이다. 자동 발행하면 전량이 blurb 없이 나간다.
사람이 발행하기 전에 **비공개 데이터 저장소를 `git pull` 해야 한다.**
2.6 이후로 이건 선택이 아니라 전제다 — 로컬에 스냅샷이 없으면 발행이 카탈로그를
직접 긁게 되고, 그러면 봇이 뜬 것과 다른 근거로 발행된다.

수집 cron은 둘이다. 시각 제한이 없는 소스는 KST 월 10:00, robots가 시각을 제한한
소스(gs25)는 KST 월 14:00에 돈다([ADR-0014](docs/adr/0014-collection-time-window.md)).

⚠️ **수집 데이터는 저장소 밖에 있다.** `THIS_WEEK_TASTE_DATA_DIR`를 비공개 데이터
저장소로 지정하고 돌린다(2.2, [ADR-0010](docs/adr/0010-repo-public-scope.md)).

**기준선 `2026-W33`, 첫 발행 `2026-W35`.** W34는 건너뛰어서 첫 diff는 2주치다
([ADR-0007](docs/adr/0007-week-gap-lookback.md)).

| 소스 | 채널 | W33 | W35 | 요청 | 설명문 | 비고 |
|---|---|---|---|---|---|---|
| cu | convenience | 4,244 | 4,008 | 110 | 상세 | 이름이 12자로 잘려 온다 |
| homeplus | mart | 2,552 | 2,467 | ~80 | **없음** | 가격 있음. 이름 온전. 신상 라벨 없음 |
| starbucks | cafe | 326 | 330 | 18 | 목록 | 가격 없음. MD는 요청하지 않는다 |
| orion | fmcg | 115 | 114 | 15 | 상세 | 가격 없음. 동명이인 2건 |
| compose | cafe | — | 197 | 15 | **없음** | 2026-08-25 추가. 가격 없음. 첫 diff는 W36 |
| baskinrobbins | dessert | — | 128 | 6 | 상세 | 2026-08-25 추가. 가격 없음. **소스가 태그를 준다** |
| dunkin | dessert | — | 216 | 20 | 상세 | 〃 가격 없음. 설명문·태그가 둘 다 상세에 |
| kyochon | restaurant | — | 101 | 4 | **목록** | 〃 가장 싸다. 가격 92/101 |
| bbq | restaurant | — | 105 | 6 | **목록** | 〃 **가격 105/105.** 상세는 클라이언트 렌더링 |
| emart24 | convenience | — | 566 | 38 | **없음** | 〃 **개별 상품 URL이 없다**([ADR-0013](docs/adr/0013-source-url-optional.md)). 가격 있음 |
| mcdonalds | restaurant | — | 100 | 7 | **목록** | 〃 가격 없음. 이름에 HTML 태그가 온다 |
| momstouch | restaurant | — | 66 | 6 | **목록** | 〃 가격 없음. 이미지가 인라인 CSS |
| bhc | restaurant | — | 113 | 5 | **목록** | 〃 가격 없음. **소스가 태그를 준다**(cateNm) |
| dominos | restaurant | — | 50 | 3 | **목록** | 〃 **EUC-KR.** 가격 M 최저가. source_url이 1층·2층 혼합 |
| parisbaguette | dessert | — | 519 | 8 | 상세 | 〃 **이 채널 최대.** 가격 없음. `admin-ajax`로 받는다 |
| gs25 | convenience | — | 567 | 4 | **없음** | 〃 가격 있음. CSRF 필요. **robots가 수집 시각을 제한한다**([ADR-0014](docs/adr/0014-collection-time-window.md)) |
| burgerking | restaurant | — | 192 | **1** | 상세 | 2026-08-26 추가. 가격 없음. **전체 카탈로그가 요청 1건이다.** 봉투를 갖춘 POST |

건수는 **범위를 좁힌 뒤의 값이다**([ADR-0012](docs/adr/0012-collection-scope.md), 2026-08-24).
CU는 생활용품 카테고리를, 홈플러스는 신선 원물 5개 분류를 뺐다. 좁히기 전은
cu 5,082/4,754(131요청), homeplus 2,966/2,861(130요청)이었다. W33·W35 스냅샷도 같이 정리했다.

"설명문" 열이 `enrich` 대상을 가른다. **홈플러스는 상세에도 설명문이 없어**(표본 51건 중
텍스트 1건) 상세를 긁지 않는다 — 스타벅스가 "목록이 이미 다 줘서" 건너뛰는 것과
이유가 정반대다. 이 소스의 `blurb`는 항상 `null`이다(6장).

소스별 값(brand·channel·detail·monotonic_key)은 `pipeline/sources.py` 표 한 곳에 있다.

`data/weeks/<week>.report.json`의 `by_source`에서 소스별로 세 지표를 보고 판단한다:

| 지표 | 무엇을 보는가 |
|---|---|
| `added` | 주당 수십~수백이면 정상. 수천이면 매칭이 깨진 것 |
| `source_new_label.added_not_labelled` | **신상이라고 판정했는데 소스는 NEW라 안 한 건수 = 오탐 후보** |
| `monotonic_id.added_below_previous_max` | 등록 순서를 거스르는 신상 = 오탐 후보. **단조 증가 키가 확인된 소스에만 실린다** |

**판단 기준은 added 건수가 아니라 오탐 비율이다.** 소스의 NEW 라벨은 판정에 쓰지 않지만
(2.1) 채점표로는 쓴다 — 이것이 원칙을 지키면서 diff 품질을 수치화하는 유일한 방법이다.

⚠️ **채점표가 없거나 못 믿을 소스가 있다.** 3단계 판단은 CU를 주된 근거로 삼는다.

| 소스 | 채점 가능? |
|---|---|
| cu | ✅ NEW 라벨 + 단조 키 둘 다 |
| homeplus | ❌ 둘 다 없다. 건수 2위인데 채점을 못 한다 |
| starbucks | ❌ **라벨은 있으나 무효.** 2026-08-24 실측: NEW 28건 중 **26건이 지난 스냅샷에도 있었고** 전부 음료다. 신상 표시가 아니라 프로모션 배지다 |
| orion | △ 라벨 8건 중 7건이 added가 아니다. 스타벅스와 같은 성질로 보인다 |
| compose | ❌ 항목 마크업에 신상 라벨 자체가 없다. 단조 키도 확인 안 됨 |
| baskinrobbins | △ `li.menu-list__item--new`가 있다(W35에 2건). **W36에 "지난주에도 붙어 있었나"를 확인하기 전에는 쓰지 않는다** |
| dunkin | △ 서브카테고리 `신제품`이 신상 표시다(DONUT에만 있다). 〃 |
| kyochon | ❌ 항목 마크업에 신상 라벨이 없다 |
| bbq | △ **이름에 `[NEW]` 접두사가 붙는다**(105건 중 8건). 〃 |
| emart24 | △ NEW 라벨이 `opacity: 0`으로 숨겨져 온다. 보이는 것만 8건(566건 중). 〃 |
| mcdonalds | ❌ `newIcon` 필드는 있으나 2026-08-25 실측에서 110건 전부 비어 있었다 |
| momstouch | △ 항목에 `i.new` 배지가 있다(66건 중 6건). 〃 |
| bhc | △ `isNew`가 있다(113건 중 19건). ⚠️ **`"Y"`/`"N"` 문자열이라 truthy로 읽으면 전건이 신상이 된다** |
| dominos | △ `div.label-box`에 NEW·기간한정·특가·시그니처 배지가 있다(50건 중 NEW 6건). 〃 |
| parisbaguette | ❌ 목록 조각에 신상 배지가 없다 |
| gs25 | △ `isNew`가 있다(567건 중 87건). ⚠️ **`"T"`/`"F"` 문자열이다.** 〃 |
| burgerking | △ `menuFlagList`에 NEW가 있다(192건 중 **46건**). 비율이 높아 판촉 배지일 공산이 크다. 〃 |

**라벨이 있다고 채점표가 되는 것이 아니다.** 쓰기 전에 "지난주에도 붙어 있었나"를
먼저 확인한다. 오래 남는 라벨은 캠페인 표시이지 신상 표시가 아니다.

**2026-08-24, 진짜로 시간이 지난 첫 diff** (W33 → W35, `gap_weeks: 2`):

| 소스 | 신상 | 단종후보 | 변경 | 보류 | 매칭 |
|---|---|---|---|---|---|
| cu | 95 | 331 | 18 | 0 | 3,913 |
| homeplus | 110 | 195 | 160 | 4 | 2,353 |
| starbucks | 9 | 5 | 1 | 0 | 321 |
| orion | 1 | 2 | 3 | 0 | 113 |

(범위를 좁히기 전 값은 cu 103/431/21, homeplus 158/263/232였다.)

`changed`와 `review`가 여기서 처음 실제 데이터로 돌았다. 그전까지는 합성 '지난주'로만
검증돼 있었다 — 가격 변경·이름 변경·진짜 단종은 그 안에 하나도 없었다.

⚠️ **`gap_weeks`를 함께 읽는다.** 2주치가 한 번에 잡히므로 건수가 그만큼 부풀어 보인다.
판단 기준은 건수가 아니라 오탐 비율이다.

**판정: 3단계 통과.** CU 기준 오탐 후보 4/103 = 3.9%. 그리고 **두 독립 지표(NEW 라벨,
단조 키)가 정확히 같은 4건을 지목했다** — 서로 다른 방법이 같은 답을 냈다는 뜻이다.

그 4건은 전부 `gd_idx`가 지난주 최댓값(27989)보다 한참 낮았다(14147~16341).
껌·헛개차·미용티슈·풀 — 오래된 상품이 목록에 돌아온 **재입고**이지 오판이 아니다.
"단종 1건 + 신상 1건"으로 갈라지는 치명적 오탐은 0건이다.

> 💡 **단조 증가 키가 재입고를 잡아낸다.** 6장 1번이 LLM에게 맡긴 "진짜 신제품 vs 재입고"
> 구분을, 그런 키가 있는 소스에서는 숫자로 할 수 있다. 지금은 지표로 보고만 하고
> 판정에는 쓰지 않는다 — 쓰려면 2.1(소스 신호를 판정에 쓰지 않는다)과의 관계를 먼저 정리해야 한다.

## 8-1. 스스로 정하는 것과 먼저 묻는 것

결정이 전부 사람에게 몰리면 그 사람이 병목이 된다. 반대로 아무거나 정하면 설계가 흔들린다.
경계를 여기 적는다.

**스스로 정하고 ADR로 보고한다**
- 파서 구현 방식, 파일 배치, 함수 분리
- 테스트를 쓸지 말지 (기준은 7장에 있다)
- 소스별 카테고리 코드, 부트스트랩 건수 같은 소스의 *내용*

**먼저 묻는다**
- 4장 스키마 변경 (필드 추가·삭제·의미 변경)
- 2.2의 저장소 경계 변경
- 새 런타임이나 의존성 도입
- 소스 우선순위 변경
- 발행을 멈출지 말지 (2.4의 이상 상황 판단)
- 7장 "실행 전에 승인을 받는 것"에 해당하는 전부

## 9. 커밋 규칙

- 데이터 커밋 메시지: `data: 2026-W32 snapshot (cu, gs25, ...)`
- 데이터 커밋과 코드 커밋을 섞지 않는다.
- **제목으로 말한다.** 본문은 코드도 ADR도 말해주지 못하는 것이 있을 때만 쓴다.
  결정의 근거는 커밋 본문이 아니라 ADR에 있다.
- 자동 커밋은 `github-actions[bot]` 명의로 남긴다.

## 10. 환경 변수

```
ANTHROPIC_API_KEY    # curate 단계용. 없으면 LLM 없이 원본 그대로 발행한다(6장)
THIS_WEEK_TASTE_LLM  # api(기본) | cli | off. cli는 `claude -p`로 구독 인증을 쓴다.
                     # cli는 로컬 전용이라 Actions에서 안 되고, 구독 사용량을 소모한다.
                     # 자동으로 cli로 넘어가지 않는다 — 명시적으로 켜야 한다
GH_TOKEN             # 실패 시 Issue 생성용 (Actions 기본 토큰으로 대체 가능)
GITHUB_REPOSITORY    # 〃 (owner/repo). GH_TOKEN과 둘 다 있어야 Issue를 만든다
THIS_WEEK_TASTE_UA   # User-Agent 전체 문자열
THIS_WEEK_TASTE_DATA_DIR  # 수집 데이터(raw/snapshots/diffs/enriched)가 사는 곳.
                     # 비우면 저장소의 data/. 발행물은 여기 해당하지 않는다 (2.2)
DATA_REPO_TOKEN      # (Actions secret) 비공개 데이터 저장소 push 권한.
                     # 권한을 그 저장소 하나로 좁힌다
```

> ⚠️ 환경변수를 **빈 문자열로 정의하는 것과 정의하지 않는 것은 다르다.**
> `os.environ.get(키, 기본값)`은 빈 문자열을 기본값으로 바꿔주지 않는다.
> 실제로 CI에서 UA가 통째로 비어 나갈 뻔했다 — `base.py`가 지금은 `or`로 되돌리고,
> 빈 UA면 세션 생성을 거부한다.

> ⚠️ `THIS_WEEK_TASTE_UA`의 기본값은 `ThisWeekTaste/1.0 (+https://example.invalid/about)`이다.
> **도메인이 확정되면 반드시 바꿔야 한다.** 5장은 연락 가능한 식별자를 요구하는데
> `example.invalid`는 연락이 불가능하다. 공개 배포 전에 고칠 것.
> 이 문자열에 `Claude`를 넣지 말 것 — 일부 사이트(bhc 등)가 `ClaudeBot`을 차단하고 있어
> 넣는 순간 금지 대상이 된다. `base.Session`이 이를 강제한다.

## 11. 아직 검증되지 않은 것 (믿지 말 것)

수직 관통은 끝났지만 **한 번도 실행되지 않은 코드 경로**가 있다. 다음 세션이 이걸 모르고
"이미 되는 것"으로 취급하면 안 된다.

| 코드 | 상태 | 검증 방법 |
|---|---|---|
| `curate.py`의 **api 경로** | 미검증. `ANTHROPIC_API_KEY`가 없어 아직 못 돌렸다 | 키를 넣고 `make publish`. cli 경로는 2026-08-12에 실측 검증했다(아래) |
| `alert.py`의 Issue 생성 | ✅ **2026-08-25에 실제 Issue를 만들어 확인했다**(#1, 확인 후 닫음). 401 실패 경로도 함께 봤다 | 끝났다. 아래 실측 문단 참조 |
| `snapshot.py`의 `_hold_previous` | ✅ **2026-08-25에 실제로 이월했다.** 위 Issue 검증과 같은 실행에서 `held_from: 2026-W34`가 붙었다 | 끝났다 |
| 급증(+200%) 탐지 | ✅ 2026-08-25에 테스트로 검증(`tests/test_snapshot_volume.py`). 실제 수집에서 걸린 적은 없다 | 경계값(정확히 300%)까지 덮었다 |
| `enrich.py`의 실패 처리 | ✅ 2026-08-25에 테스트로 검증(`tests/test_enrich_failure.py`) — 한 건 실패해도 나머지가 보강되고, 실패 항목은 **빈 설명문으로 덮이지 않고 아예 빠진다** | 실제 수집에서는 아직 실패한 적이 없다 |
| `MAX_DETAIL_FETCHES` 상한 | 테스트로만 검증. 실제로 걸린 적 없다(최대 158 < 500) | 근거가 없는 값이다. 여러 주 실측 후 조인다 |
| 스냅샷의 `tags` | 2026-08-25에 스키마에 넣었다. 배스킨라빈스 128/128이 태그를 갖고 있다 | **발행까지 가본 적이 없다.** W36에서 `tags`가 실제로 카드에 실리는지 확인 |
| 단종 이월 | **W35가 첫 발행이라 지난주 발행본이 없었다.** `discontinued: 0` | W36. CU만 431건이 대기 중이다 |
| `curate.py`의 분류 목록 | 2026-08-25에 **6채널 전부 채웠다**(dessert 14종, restaurant 13종). 실제 카탈로그 550건을 보고 만들었다 | ⚠️ **LLM에 돌려본 적이 없다.** W36 발행에서 `기타` 비율과 목록 밖 값을 확인할 것 |
| `base.Session`의 `Visit-time` 거부 | ✅ **2026-08-25에 실제로 거부됐다.** 창이 닫힌 1분 뒤(KST 17:46)에 돌려 확인했다. `Crawl-delay`도 gs25 실수집에서 10초 간격이 실제로 걸렸다 | 끝났다. 아래 실측 문단 참조 |

반대로 **실제 데이터로 검증된 것**: 스냅샷 4소스 8,059건, external_id 유일성 검사
(바코드 중복 16건을 실제로 잡아냈다), 카테고리 건수 검증 양쪽 경로, 요청 간격 1초 준수.

**2026-08-12 추가 검증** — `curate.py`의 cli 경로를 실제 스냅샷 20건으로 돌렸다.
상세 20/20 확보, blurb 20/20 생성(전부 40자 이내, 전부 원문 근거), 자체 분류 18/20,
근거 없는 blurb 0건. 5회 반복해 결과가 일관됨을 확인했다. 이 과정에서 **조용히 실패하는
경로 세 개**를 찾아 고쳤다 — 응답이 맨 배열일 때 `AttributeError`로 발행 전체가 죽던 것,
빈 결과를 성공으로 취급해 전량이 조용히 원본으로 나가던 것, 일부 항목 누락이 무기록이던 것.

**2026-08-13 추가 검증** — 홈플러스 스냅샷 2,966건(130요청, 108개 카테고리 전부 기준 통과),
`enrich.py`의 **상세 없는 소스 경로**(3/3건을 실패로 세지 않고 건너뛴 뒤 빈 결과 저장).
브라우저 확인 1회로 상세 URL과 이미지 URL 규칙을 실측했다(`sources/targets.yml` 참조).

**2026-08-24 — 첫 실제 발행.** 여기서 처음으로 **시간이 진짜 지난 뒤의 diff**가 돌았다.
그전 검증은 전부 합성 '지난주'였고, 거기엔 가격 변경·이름 변경·실제 단종·상품 코드
재발급이 하나도 없었다.

이번에 처음 실제 데이터로 돈 것:
- **`changed` 경로** — 홈플러스 232건(마트라 가격 변동이 잦다). 이게 없었으면
  232건이 "단종 232 + 신상 232"로 오탐됐다
- **`review` 경로** — 홈플러스 6건. 유사도는 넘는데 확신이 안 서는 것들을
  added/removed 어느 쪽에도 넣지 않고 뺐다(2.1)
- **주차 되짚기** — W34가 없어 W33과 비교. 검증·이월·diff 세 곳 전부([ADR-0007](docs/adr/0007-week-gap-lookback.md))
- **소스별 발행 분리와 병합** — 소스 4개가 정확히 합쳐졌다([ADR-0008](docs/adr/0008-published-parts-and-merge.md))
- **`enrich`의 세 갈래** — 상세 있음(cu 103) / 상세 없는 소스(homeplus 158 건너뜀) /
  목록이 이미 줌(starbucks 9)
- **`curate.py` cli 경로를 271건 규모로** — blurb 113건(설명문이 있는 113건 전부),
  홈플러스 158건은 근거가 없어 전부 `null`. 지어낸 것 0건
- **정적 빌드** — 카드 271 == 항목 271, blurb 113 일치

**2026-08-24 추가 검증 (같은 날 오후)** — 2.6의 멱등과 근거 기록.
오리온으로 실측했다: 재사용 경로는 요청 0건에 0.07초, `REFRESH=1`은 실제로 15요청을 보낸다.
발행 리포트의 `snapshot` 블록은 W35 실제 데이터로 4소스 전부 확인했다.

이 과정에서 **`_hold_previous`가 무조건 덮어쓰던 것**을 찾아 고쳤다. `--refresh`로 다시 뜨다가
이상 상황에 걸리면 검증을 통과했던 이번 주 스냅샷이 지난주 것으로 되돌아갔다.
흔적은 `held_from` 하나뿐이라 조용하다. 이제 이번 주에 정상 스냅샷이 있으면 이월하지 않는다.

**2026-08-25 — 이상 상황 처리의 전 경로를 실제로 돌렸다.** 11장이 "한 번도 실행 안 됨"으로
적어둔 두 항목이 한 번에 검증됐다. 임시 데이터 디렉토리에 합성 W34 스냅샷(교촌 10건)을 놓고
실제 W35를 수집해 카테고리 건수 이상을 유도했다 — 실데이터는 건드리지 않았다.

- **Issue가 실제로 만들어졌다**(#1). 제목·본문·`pipeline` 라벨이 그대로 붙었다
- ⚠️ **없는 라벨은 GitHub이 자동으로 만든다.** 저장소에 `pipeline` 라벨이 없어서 422가
  날 것으로 예상했는데 아니었다. 라벨을 미리 만들어 둘 필요가 없다
- **잘못된 토큰이면 401을 로그로만 남기고 파이프라인은 계속 간다.** Issue도 안 만들어진다
- **`_hold_previous`가 처음으로 실제 이월을 했다** — `held_from: 2026-W34`, 10건.
  그전 두 번의 호출은 이월할 것이 없거나 가드에 막혔다(둘 다 의도된 동작)

**2026-08-25 오후 — gs25를 붙이고 수집 시각 창을 실제로 확인했다**([ADR-0014](docs/adr/0014-collection-time-window.md)).

- **창 안(KST 16:01)에서 567건을 실제로 수집했다.** 4요청, 부트스트랩 8개 분류 전부 통과.
  `Crawl-delay: 10`이 실제로 걸려 요청 사이가 10초였다
- **창이 닫힌 1분 뒤(KST 17:46)에 돌려 `VisitTimeClosed`로 거부되는 것을 봤다.**
  임시 데이터 디렉토리를 써서 실데이터는 건드리지 않았다
- ⚠️ **창 밖에서도 `robots.txt` 요청 하나는 나간다.** 창을 알아내려면 읽어야 하기
  때문이다. 상품 요청은 그 뒤에 막힌다 — 막는 지점이 `assert_allowed`라서다

**2026-08-14 추가 검증** — 채널별 분류 목록을 cli 경로로 3채널 60건에 돌려
기존 프롬프트와 비교했다([ADR-0006](docs/adr/0006-category-taxonomy-per-channel.md)).

| 소스 | 기존(편의점 17종) | 채널별 |
|---|---|---|
| homeplus (mart) | **기타 6/20**, 9종으로 분산 | 기타 0/20, 13종 |
| starbucks (cafe) | 기타 0, 그러나 4종뿐(커피8·음료6·디저트4·샌드위치2) | 기타 0, 8종 |
| orion (fmcg) | 기타 0, 사실상 1종(과자17·디저트3) | 기타 0, 6종 |

마트는 `기타`가, 카페·FMCG는 **분류가 뭉개지는 것**이 문제였다. 오리온 20건이 전부
`과자`로 떨어지면 사이트 필터가 아무 일도 하지 못한다. 목록 밖 값은 60건 중 0건이라
목록 검증 코드는 넣지 않았다 — 근거가 없다.

이 과정에서 **상품 키를 LLM과 주고받던 것이 홈플러스에서 깨져 있었다**는 것을 찾아 고쳤다
(6장). 고치기 전 홈플러스는 20건 중 0건이 편집됐고, 고친 뒤 20/20이다.
발행은 원본 폴백으로 계속되므로 **아무 증상 없이 그 소스만 편집에서 빠져 있었다.**
